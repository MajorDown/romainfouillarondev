const SendMail = () => {
  return <div>formulaire de mail</div>;
};

const sendMailDiv = document.getElementById("sendMail");
const root = ReactDOM.createRoot(sendMailDiv);
root.render(<SendMail />);
